// Ruta: /moderacion/ofertas — bandeja de revisión de vacantes ya publicadas
// (post-moderación, DEC-01). Placeholder inicial. Owner: (asignar).
//
// Bajó un nivel desde /moderacion: todas las pantallas de admin cuelgan del
// prefijo /moderacion/*, y el nav no puede tener un href que sea prefijo de
// otro (ver el comentario en components/layout/nav-items.ts).
export default function OfertasPage() {
  return <h1>Moderación</h1>;
}
