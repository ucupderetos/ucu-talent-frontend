"use client";

// Vista de perfil del alumno. Se monta desde /perfil (route group compartido
// (perfil)) cuando el rol es ALUMNO — ver app/(perfil)/perfil/page.tsx.
//
// 🚧 Placeholder: la pantalla del perfil de alumno todavía no está construida.
// Owner: grupo de alumno. Al implementarla, la lógica de datos va en
// features/perfil/hooks/ (useQuery), no acá.

export function StudentProfileView() {
  return <h1>Perfil</h1>;
}
