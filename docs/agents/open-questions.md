# Pendiente de aclarar / estado de definición

> Parte de [AGENTS.md](../../AGENTS.md) — ver el índice ahí para el resto de los documentos.

> **Actualizado 2026-07-27** contra `docs/ENDPOINTS.md` — el contrato funcional cerrado
> recibido de backend, transcripto en ese archivo. Reemplaza como fuente a las
> confirmaciones sueltas que citaba esta sección antes de que el documento existiera.
> Leyenda:
> - **✅ Resuelto** — definido y verificable hoy; se programa contra esto.
> - **🔄 Confirmado, aún no en `api-dev`** — el backend confirmó la decisión pero
>   `ENDPOINTS.md`/`api-dev` todavía no la reflejan. **Se trata como el contrato vigente
>   (se programa contra ella), pero se verifica al integrar** — no asumir que `api-dev` ya
>   lo tiene.
> - **🔴 / 🟡 Abierto** — sigue sin definirse. Si una tarea depende de uno, se frena y se
>   pregunta.

## ✅ Resuelto o confirmado (contrato vigente)

| # | Estado | Qué aplica |
|---|---|---|
| **A-01** | ✅ | **Confirmado directo contra el código fuente del backend** (`UserServiceImpl.create`, no contra prosa): el alumno nace `PENDIENTE` (igual que la empresa). Hace todo lo normal (perfil, feed, detalle) **excepto postularse**; un **Admin lo aprueba a mano** contra el padrón (`UniversityRegistry`) → `APROBADO` o `RECHAZADO`, vía `PATCH /user/{id}`. Solo `APROBADO` puede postularse. Implica que el admin construye la **cola de aprobación de alumnos** (RF-MOD-05/06). El RBAC de arriba queda **correcto**. Ver [Roles y control de acceso](roles-and-access-control.md) para el aviso sobre por qué esto contradice al `ENDPOINTS.md` del propio repo de backend (esa doc está mal, no el comportamiento). |
| **A-02** | ✅ | Moderación **existe**: `PATCH /user/{id}` (status + `adminComment`, se guarda en `StudentProfile`/`Company`), `PUT /vacancy/status/{id}` (admin), `reviewedAt`, y `*/status-summary`. El dominio `moderacion` ya puede escribir hooks. |
| **A-03** | ✅ | `skills` es `string[]` (solo en `StudentProfile`). El orden por coincidencia (RF-FEED-01) **se descarta** — `Vacancy` no lleva `skills`. |
| **A-04** | ✅ | **`GET /vacancy` (el que usa hoy el feed) NO se pagina, y no va a hacerlo** — `docs/ENDPOINTS.md` es explícito (secciones 1 y 5): devuelve la colección completa sin paginación de servidor. Front resuelve filtro, orden y paginación visual enteramente en memoria — ya implementado así en `use-feed-vacancies.ts`. ⚠️ **Matiz nuevo, verificado contra el código fuente del backend**: SÍ existe paginación real de servidor, pero en un endpoint DISTINTO que el front todavía no usa — `GET /vacancy/search` (ADMIN) y `GET /vacancy/student/search` (autenticado, ya filtra por `PUBLICADO`), los dos devuelven `Page<VacancyResponse>` con filtros combinables (área con subáreas, carrera, tipo de contrato, modalidad, localidad, keyword) y orden (`sortBy`/`sortDirection`). Ninguna versión de `ENDPOINTS.md` (ni la local ni la del backend) lo documenta. Migrar el feed a `/vacancy/student/search` reemplazaría buena parte del filtrado/paginado en memoria de A-05 — es una mejora real disponible, pero no se adoptó en esta pasada (cambia toda la capa de fetching del feed, alcance para otra tarea). `Paginated<T>` (`types/index.ts`) sigue existiendo como view model **in-memory del front** para las pantallas que ya lo simulan (tablas de postulantes, moderación, "Mis ofertas") — no está atado a ninguno de los dos casos de arriba. |
| **A-05** | ✅ | El **filtrado del feed queda en el front** por ahora (fetch-all + en memoria, ver [Guía de estilo de componentes](design-system.md) — "Barras de filtros"). ⚠️ Ya no es del todo cierto que no hay "endpoint de filtros del backend" — ver el matiz de A-04, `GET /vacancy/student/search` sí filtra combinado del lado del servidor. Se documenta acá igual porque el front no lo adoptó todavía. |
| **A-08** | ✅ | `PUT /student-profile/{id}` existe (dueño): edita `phoneNumber`, `linkedinUrl`, `skills`, `description`. `name`/`surname`/documento **no** se editan por ahí. |
| **A-09** | ✅ | `hasProfile: boolean` confirmado en `MeResponse` (`docs/ENDPOINTS.md`). `use-session.ts` hoy lo deriva del `404` del perfil — sigue siendo correcto, es candidato a simplificarse leyendo el campo directo. `name` **no** se agrega — el navbar sigue con el 2º fetch al perfil, es el diseño definitivo. |
| **A-10** | ✅ | Documento **único por el par `(documentType, documentNumber)`**. La **validez** del documento (dígito verificador, etc.) la valida el **backend**; el front solo valida superficie (cantidad y tipo de caracteres). **Implementado**: `lib/validators.ts` (`isValidDocumentNumber`) — cédula/DNI solo dígitos, exactamente 8; pasaporte alfanumérico 6–9, limpiando puntos/comas/guiones/espacios antes de medir. Lo consumen los dos formularios del paso 2 (`register-form.tsx`, `complete-profile-form.tsx`), que muestran el tipo como `Select` y bloquean el avance con mensaje inline. |
| **A-12** | 🔄 | Gaps de autorización **corregidos en backend** — falta actualizar `api-dev`. Asumir ownership/roles aplicados. |
| **A-14** | ✅ | `VacancyStatus = PENDIENTE, PUBLICADO, FINALIZADO`, default **`PUBLICADO`** (post-moderación). **Sin `RECHAZADO`.** Dos endpoints separados por actor: `PATCH /vacancy/status/{id}` (EMPRESA + dueña, cierre) y `PUT /vacancy/status/{id}` (ADMIN, `PUBLICADO ↔ PENDIENTE`). **El Admin NUNCA llega a `FINALIZADO`** — "dar de baja" para el Admin es `PUBLICADO → PENDIENTE`, corrigiendo lo que decía una versión anterior de esta fila. Ver [Roles y control de acceso](roles-and-access-control.md). |
| **A-15** | ✅ | **`contractType` es un enum real**, no `string` libre — corrige lo que decía esta fila antes (que el contrato no lo confirmaba): `vacancy/ContractType.java` en el backend define `FULL_TIME, PART_TIME, FREELANCE, PASANTIA, CONTRATO_FIJO, CONTRATO_INDEFINIDO, SUPLENCIA, BECA`. Verificado contra el código fuente, no contra ningún `ENDPOINTS.md` (ninguna versión, ni la local ni la del backend, lo documentaba como enum). `types/index.ts` ya tiene `ContractType`, y el catálogo + diccionario de presentación vive en `lib/contract-types.ts` (`CONTRACT_TYPES`, `CONTRACT_TYPE_LABELS`, `CONTRACT_TYPE_OPTIONS`). El input de texto libre de "tipo de contrato" se reemplazó por un `Select` en los formularios que lo editan (`job-basic-info-form.tsx`, `edit-job-form.tsx`) y en el filtro del feed. ⚠️ **Revertido 2026-07-30 — el campo de sueldo YA NO tiene un nombre distinto según el
endpoint.** Una versión anterior de esta fila (verificada 2026-07-29 "a mano contra
Swagger") decía que `UpdateVacancyRequest` usaba `salaryRange` en vez de `salary`, y
`use-edit-job.ts` traducía `salary` → `salaryRange` en el borde de red por eso. Verificado
de nuevo 2026-07-30 **directo contra el código fuente actual**
(`vacancy/dto/UpdateVacancyRequest.java`, rama `dev`): el campo es `salary`, sin
`salaryRange` en ningún lado del DTO — igual que `CreateVacancyRequest`. El backend debe
haber corregido esa inconsistencia entre el 29 y el 30. Se sacó la traducción de
`use-edit-job.ts`: ahora manda `salary` tal cual, igual que el `POST`. Con la traducción
vieja, el `PUT /vacancy/{id}` mandaba un `salaryRange` que el DTO real ignora — el salario
dejaba de actualizarse en silencio (bug real, corregido en esta pasada). Moraleja: cuando
el código fuente contradice una verificación empírica anterior, gana el código fuente
actual — el backend puede haber cambiado desde la última vez que se probó a mano. `salary` sigue siendo un único `string` en el wire — tanto "Publicar oferta" (`job-details-form.tsx`) como "Editar oferta" (`edit-job-form.tsx`) lo parten en 3 controles de UI (moneda UYU/USD + monto desde + monto hasta opcional), con las utilidades compartidas en `lib/salary.ts` (`formatSalary`/`parseSalary`/`SALARY_CURRENCIES`/`SALARY_MODES`/`SALARY_AMOUNT_PATTERN`), armando/parseando el string al enviar/precargar. La **creación es solo estructurada** (siempre monto numérico, sin texto libre): el modo "texto libre" de respaldo (vía `salaryMode`, para rescatar valores legados no numéricos como "A convenir") existe **solo en edición**, donde `parseSalary` lo activa si no puede rescatar un monto de un dato viejo — al crear no hay valor previo que preservar, así que ese modo no se ofrece. `location` sigue como campo requerido en `CreateVacancyRequest` — el contrato no confirma la nulabilidad condicional a `REMOTO` que se anticipaba acá; tratar como no resuelto. Sin orden por skills. Ver también el aviso de `publicationDate`/`closingDate` (obligatorias, no autogeneradas) en [Roles y control de acceso](roles-and-access-control.md). |
| **A-17** | ✅ | **Reversión de una reversión — `accepted` (ex-`selected`) SÍ existe.** Una versión anterior de esta fila lo daba por eliminado porque ninguna versión de `ENDPOINTS.md` lo documentaba; verificado contra el código fuente del backend (no contra esa doc): `VacancyApplicationResponse.accepted` (boolean) es real, se marca vía `PATCH /vacancy-application/{id}/accept` (empresa dueña) y define el contenido del mail de cierre. `types/index.ts` (`VacancyApplication.accepted`) ya lo tiene. Sigue sin viajar en `VacancyApplicationStudentResponse` (`GET /vacancy-application/me`) — el alumno sigue sin verlo, la barra de progreso de "Mis postulaciones" sigue sin mostrarlo, y eso sigue siendo correcto. Ver [Postulaciones](applications-state-machine.md). |
| **A-18** | ✅ | `CompanyResponse` expone `status`, `reviewedAt`, `adminComment` (y `StudentProfileResponse` también, más `description` — ver [Roles y control de acceso](roles-and-access-control.md)). El admin ve/filtra por estado. |
| **A-19** | ✅ | Error `application/problem+json` con mapa por campo bajo la key **`errores`**: `{ detail, title, status, instance, errores: { campo: mensaje } }`. Mapear `errores` a `setError` de RHF y tipar así `ApiError`. Implementado en `lib/api-client.ts` (`errorMessage` lee `detail`, `ApiError.fieldErrors` expone `errores`). `ApiError.fieldErrors` ya se mapea a `setError` en "Editar oferta" (`EditVacancyView`/`EditJobForm`) y en la creación (`app/(empresa)/crear-oferta/revision/page.tsx`). `salary` es el único campo sin mapeo 1 a 1 en "Editar oferta" (está partido en moneda/mínimo/máximo o texto libre, ver A-15): cae en un error general al pie del form (`errors.root`) en vez de adivinar qué control corregir. |
| **A-06** | ✅ | **Resuelto: NO es una decisión de front — el backend bloquea la edición entera.** Corrige lo que decía esta fila antes ("el backend no bloquea"). `VacancyServiceImpl.updateVacancy` (fuente del backend): si la vacante tiene aunque sea una postulación (`vacancyApplicationRepository.existsByVacancyId(id)`), `PUT /vacancy/{id}` devuelve `403 "El Puesto ya tiene postulaciones."` — no hay edición parcial de "solo estos campos siguen editables", es todo o nada. También bloquea si la vacante ya está `FINALIZADO`. El front repite el mismo gate como UX (no como seguridad, ver [Roles y control de acceso](roles-and-access-control.md)): `vacancy-table.tsx` **oculta el lápiz de "Editar"** de "Mis ofertas" cuando la vacante está `FINALIZADO` o tiene **>=1 postulaciones**, y `EditVacancyView`/`EditJobForm` (`isLocked`, conteo vía `useVacancyApplicantsCount` — `GET /vacancy-application?vacancyId={id}`) repiten el mismo gate con campos en solo lectura y la explicación en pantalla, por si se llega por URL directa. Un conteo de postulantes que falla (`isError`) se trata como "tiene postulantes" (`applicantsCountKnown: boolean`), nunca como "0" — un conteo desconocido no es lo mismo que "sin postulantes", y ese fallback dejaría editar (o mostraría el lápiz de) una oferta que en realidad puede tener postulantes. |
| **A-07** | ✅ | **Resuelto: SÍ, el backend lo exige.** Corrige lo que decía esta fila antes ("el backend no lo valida, decisión de front"). `VacancyApplicationServiceImpl.create` devuelve `409` ("El alumno debe tener al menos un registro de educacion para postularse") si `EducationService.getByStudentProfileId` viene vacío. El front no lo pre-valida — se apoya en que `use-vacancy.ts` ahora muestra el `detail` real del backend en vez de un mensaje genérico (ver [Postulaciones](applications-state-machine.md)). |
| **A-13** | ✅ | **CORS.** Confirmado OK para `http://localhost:3000` desde el principio. `https://dev.ucutalent.tech` (el frontend deployado) estuvo unas horas fuera de la whitelist el 2026-07-28 (`403 Invalid CORS request` hasta en `GET` simples) — infra lo arregló ese mismo día, reconfirmado con `curl` ~20:26 ART: preflight da `200` + `Allow-Origin` correcto, y `GET /area` pasa el CORS y llega a auth. Lo que queda de esta fila **no es CORS**: confirmar los atributos del `Set-Cookie` (`SameSite=None; Secure`) con un login real — no probado todavía. |
| **A-21** | 🔄 | **Observado 2026-07-29, sin coordinar con backend todavía**: la respuesta real de `GET /vacancy` trae `publicationDate`/`closingDate`/`createdAt`/`deletedAt`/`deleted`/`reviewedBy`, y no trae `finalizedAt` (ver `docs/ENDPOINTS.md`, sección 5). `publicationDate`/`closingDate` ya son campos **requeridos** en `Vacancy` (`types/index.ts`) — se sacaron `publishedAt`/`finalizedAt`, y todos los consumidores (feed, moderación, "Mis ofertas") leen `publicationDate`/`closingDate` directo, sin fallback. |
| **A-16** | ✅ | **Nombre de la cookie de sesión: `access_token`.** Verificado 2026-07-30 directo contra el código fuente del backend (`auth/AuthController.java`, `auth/CookieBearerTokenResolver.java`, rama `dev`) — no hizo falta el login real desde `https://dev.ucutalent.tech` que estaba pendiente. También confirma de nuevo los atributos: `httpOnly`, `secure`, `sameSite=None`, `path=/`. ⚠️ **Ningún código del front la lee ni puede leerla**: antes la usaba `proxy.ts`, que se borró el 2026-07-30 justamente porque el server del frontend no puede verla (vive en el dominio de la API) y su guard causaba un loop de redirección. El dato del nombre queda registrado para debugging, no porque se use. |
| **A-22** | 🔄 | **`GET /vacancy/company/{companyId}/management`** (`VacancyManagementResponse`: `vacancy` + `companyName` + `areaName` + `applicationCount` + `newApplicationsCount`) — endpoint agregado real, no documentado en ninguna versión de `ENDPOINTS.md` (verificado 2026-07-30 contra `vacancy/VacancyController.java`/`VacancyServiceImpl.java`/`VacancyRepository.java` del backend). Ya filtra `deleted = false` del lado del servidor. `use-company-vacancies.ts` migró a este endpoint, eliminando el N+1 anterior (`GET /vacancy` + un `GET /vacancy-application` por vacante). `newApplicationsCount` cuenta postulaciones en `PENDIENTE` ("sin revisar"), no "postulado en los últimos 7 días" — el badge de `vacancy-table.tsx` se actualizó a ese criterio. |
| **A-23** | 🔄 | **El backend SÍ tiene `MailTemplateController`/`Service`/`Repository` completos y funcionando** (`GET`/`PUT /mail-template`, verificado 2026-07-30 contra el código fuente) — contradice la sección [Mails](mails.md) de este archivo, que da a `MailTemplate` por eliminada porque el MER (fuente #2) no la tiene. Decisión del equipo 2026-07-30: **mantener la ABM fuera de alcance** (no implementar RF-PUE-05) hasta confirmar si es código legacy que el backend no terminó de retirar, o un cambio de rumbo real. No tratar como pedido de producto sin volver a confirmarlo. |
| **A-24** | ✅ | **Archivos: foto de perfil (los 3 roles) y CV (alumno).** Verificado contra el código fuente (`UserController`/`UserServiceImpl`, `StudentProfileController`/`StudentProfileServiceImpl`, `StorageServiceImpl`); ninguna versión de `ENDPOINTS.md` lo documenta. ⚠️ **Los paths NO son simétricos** — corrige una versión anterior de esta fila, que daba los tres como `/user/profile/image`: `PATCH`/`DELETE /user/profile/image` (con barra) pero `GET /user/profile-image` (con guion). El CV sí es parejo: `PATCH`/`DELETE`/`GET /student-profile/cv`. **Son dos pasos, no uno**: los campos `UserResponse.profileImage` y `StudentProfileResponse.cvFile` traen la **key del objeto en el storage, no una URL** (`{carpeta}/{UUID}.{ext}`), y se canjean por una URL firmada con `GET .../profile-image?profileObject={key}` / `GET /student-profile/cv?cvFile={key}`, que devuelven **texto plano**, no JSON. La URL la firma **Google Cloud Storage** (V4) con TTL igual a lo que le queda al token de sesión, con techo de 7 días. ⚠️ **`MeResponse` NO trae la key** (ni la foto ni nada de archivos) y **`CompanyResponse` no tiene ningún campo de imagen**: la foto de una empresa es la de su `User`, así que para mostrarla hace falta `GET /user/{companyId}` — por eso no se muestran logos en el feed ni en las cards (serían 2 requests por empresa, el mismo N+1 que se corrigió en otras pantallas). `PATCH` acepta **solo `image/jpeg` y `image/png`** (ni webp ni gif) y **el backend no valida tamaño** — el techo de 5 MB lo pone el front (`features/perfil/hooks/use-profile-image.ts`). Los tres verbos operan siempre sobre el usuario del token (`jwt.getSubject()`), no reciben id, y el `PATCH` **reemplaza**: sube el nuevo y después borra el viejo del storage, con rollback si falla. **Implementado (foto)**: `hooks/use-profile-image.ts` (lectura, app-wide), `features/perfil/hooks/use-profile-image.ts` (mutaciones + validación), `features/perfil/components/profile-image-uploader.tsx`, montado en el perfil de alumno y de empresa, y consumido por el Navbar, el detalle de postulante y las pantallas de moderación (alumnos y empresas: los dos listados, las dos colas de validación y el detalle de empresa). ⚠️ **Matiz sobre el N+1 de arriba**: lo que lo hace caro es el `GET /user/{id}` por fila, no la firma. Donde el hook ya lee el `User` por otro motivo (los listados de moderación lo cruzan para email/fecha de registro), la key sale gratis y la fila la arrastra — ahí la foto se muestra usando `useSignedProfileImageUrl(key)`, nunca `useProfileImage(id)`. El feed y las cards públicas siguen sin logo porque ahí sí no hay `User` a mano. **ADMIN todavía no tiene dónde subirla** (no tiene pantalla de perfil) — queda pendiente. **Implementado (CV)**: `hooks/use-cv.ts` (lectura, app-wide), `features/perfil/hooks/use-cv.ts` (mutaciones + validación), `features/perfil/components/cv-uploader.tsx` en "Información personal" del perfil de alumno, y `CvAction` dentro de `applicant-detail-view.tsx` del lado de la empresa — ver A-11. **Detalles del CV verificados 2026-07-30 contra el código fuente** (`StudentProfileController`/`StudentProfileServiceImpl`, rama `dev`): el campo multipart del `PATCH` se llama **`file`** (`@RequestPart("file")`, igual que la foto pero es otro controller), el `PATCH` devuelve `StudentProfileResponse` completo, **solo `application/pdf`** (`ALLOWED_CV_CONTENT_TYPES` es un `Set.of` de un elemento; otra cosa da `400` "Solo se permite PDF para el CV"), y **tampoco valida tamaño** — el techo de 5 MB lo pone el front. La key tiene la forma `student-profiles/cv/{uuid}.pdf`. ⚠️ **El nombre original del archivo se pierde**: `StorageServiceImpl.buildObjectName` arma la key como `{carpeta}/{UUID.randomUUID()}{extensión}` y no guarda `originalFilename` en ningún lado, así que ninguna pantalla puede mostrar cómo se llamaba el PDF que subió el alumno — `cv-uploader.tsx` muestra un rótulo derivado de la extensión ("CV.pdf"). Mostrar el nombre real necesita un cambio de backend (persistir `originalFilename` en `StudentProfile`). ✅ **Actualización 2026-07-30 (tarde): la firma de URLs YA funciona, al menos para el CV** — QA abrió el PDF de un postulante desde la vista de empresa contra el entorno real, o sea que `GET /student-profile/cv` devuelve una URL firmada válida. Falta reconfirmar `GET /user/profile-image` (mismo `getSignedUrl`, probablemente ande también). Los estados de "guardado pero no se puede mostrar" siguen en el código a propósito, por si reaparece. Lo que sigue queda como registro del síntoma: ⛔ **`api-dev` no podía FIRMAR URLs esa mañana** (verificado contra el entorno real, desde `localhost:3000` y desde `https://dev.ucutalent.tech`, mismo resultado): `GET /user/profile-image` devuelve **`503`** con `detail` "El almacenamiento de archivos no está configurado en este entorno" — el `catch` de `StorageServiceImpl.getSignedUrl`. **La subida SÍ funciona** (`PATCH` responde `200` y la key queda guardada en `UserResponse.profileImage`); lo que falla es el canje por la URL. Causa probable, para backend: firmar V4 necesita una clave privada de service account, y `storage.create()` anda con las credenciales por defecto del entorno mientras `storage.signUrl()` no. **Afecta igual al CV** (mismo `getSignedUrl`), así que la descarga tampoco va a andar hasta que se configure. El front ya lo maneja sin reintentar (`retry: false`) y avisando en el uploader que la imagen está guardada pero no se puede mostrar — no hay nada más que arreglar de este lado. |
| **A-25** | 🔴 | **El `GET` del CV no valida rol ni ownership** — reportar a backend. **Reconfirmado 2026-07-30** contra `StudentProfileServiceImpl.getCvFile` en `dev`: sigue igual, sin `@PreAuthorize` ni chequeo de dueño. Quien lo implementó documentó que "solo las empresas pueden" ver el CV, pero `StudentProfileServiceImpl.getCvFile` no tiene `@PreAuthorize` ni chequeo de dueño: lo único que verifica es que exista algún perfil con esa key (`existsByCvFile`) y que el token no esté vencido. O sea que **cualquier usuario autenticado con la key obtiene la URL firmada**, y la key viaja en `StudentProfileResponse.cvFile`, cuyo `GET /student-profile/{id}` es accesible a cualquier autenticado — un alumno puede leer el CV de otro. Mismo caso en `UserServiceImpl.getProfileImage` (sin ownership), aunque ahí el impacto es menor. No lo puede arreglar el front (la autorización real es del backend, ver [Roles y control de acceso](roles-and-access-control.md) — "El acceso se valida en tres capas"). **Consecuencia de UI, distinta del problema**: el alumno **sí** puede ver y descargar su propio CV, así que la pantalla de perfil no tiene que asumir lo contrario. |
| **A-26** | ✅ | **`GET /vacancy-application/me/detailed`** — DTO específico para "Mis postulaciones" (alumno), pedido a backend 2026-07-30 y **confirmado el mismo día contra el `docs/ENDPOINTS.md` del repo de backend** (`ucupderetos/ucu-talent-backend`, rama `dev`): el endpoint existe, restringido a rol `ALUMNO`, y el schema coincide exactamente con lo que consume el front. Item: `{ application: { vacancyApplicationId, vacancyId, vacancyName, companyId, companyName, appliedAt, status, vacancyStatus }, vacancy: VacancyResponse, companyName, areaName }` — sin `accepted` (mismo criterio que A-22, `.../management`), resuelve todo del lado del servidor. Reemplaza el `GET /vacancy-application/me` + `GET /vacancy` + `GET /company` + `GET /area` de antes (`use-my-applications.ts`), eliminando ese N+1 también acá. ⚠️ Esta verificación es contra la copia de `ENDPOINTS.md` del propio backend, no contra su código fuente (`*ServiceImpl`/DTOs) — mismo matiz que en el resto de esta tabla: esa copia es más detallada que la local pero tampoco es infalible (ver [Fuentes y precedencia](sources-and-precedence.md), Fuente #4). Sigue sin estar documentado en la copia local de este repo (`docs/ENDPOINTS.md`). |
| **A-27** | ✅ | **`GET /vacancy/{id}/resolved`** — detalle de vacante con las relaciones ya resueltas, **confirmado 2026-07-30 contra el `docs/ENDPOINTS.md` del repo de backend** (`ucupderetos/ucu-talent-backend`, rama `dev`). Response (`ResolvedVacancyResponse`): `{ vacancy: VacancyResponse, company: CompanyPublicResponse, areaName: string, parentAreaName: string | null }`; permiso 🔒 autenticado (cualquier rol), `404` si la vacante no existe. Reemplaza el `GET /vacancy/{id}` + `GET /company/{companyId}` + `GET /area` que armaba `use-vacancy.ts`, mismo criterio que A-22/A-26. ⚠️ **`company` es `CompanyPublicResponse`, no `CompanyResponse`**: NO trae `reviewedAt` ni `adminComment` (el `GET /company/{id}` que este endpoint reemplaza sí los traía). Por eso `types/index.ts` ahora tiene `CompanyPublic` (sin los campos de moderación) y `Company extends CompanyPublic` — tipar el DTO resuelto como `Company` prometía campos que en runtime no llegan. Mismo matiz que A-26: verificado contra la copia de `ENDPOINTS.md` del backend, no contra su código fuente. Sin documentar en la copia local (`docs/ENDPOINTS.md`), cuya sección 8 todavía dice que el detalle sale de las tres llamadas separadas. |
| **A-28** | ✅ | **`GET /admin/dashboard`** — totales y listados de la pantalla inicial del panel de Admin en una sola request, **confirmado 2026-07-30 contra el `docs/ENDPOINTS.md` del repo de backend** (`ucupderetos/ucu-talent-backend`, rama `dev`). Permiso 🔒 rol `ADMIN`. Response (`AdminDashboardResponse`): `{ counts: { companies: { total, pendientes }, vacancies: { total, publicadas }, applications: { total, pendientes }, users: { total, alumnos, empresas, admins } }, applicationStatusSummary: { status, count }[], recentVacancies: { vacancyId, name, companyName, publicationDate, status, applicationCount }[], pendingCompanies: { companyId, name, industry, registeredAt }[] }` — ⚠️ los campos de `counts` van **en español** (`pendientes`/`publicadas`/`alumnos`/`empresas`/`admins`), a diferencia del resto del contrato: son nombres de campo, no valores de enum, así que acá el backend rompe su propia convención (ver [Idioma del código](language-conventions.md)) y el front los espeja tal cual. Comportamientos confirmados de los que depende la pantalla: `vacancies.total` **excluye borradas lógicamente** (`deleted = true`), `recentVacancies` viene **capado a 5** ordenado por `publicationDate` DESC y sin borradas, `pendingCompanies` **capado a 10** por `registeredAt` DESC, y `applicationStatusSummary` **siempre trae los 3 estados** aunque den 0. Reemplaza al enfoque anterior de `use-dashboard.ts` (traer `/company` + `/user` paginado completo + `/vacancy` + `/vacancy-application` y agregar en el front), mismo criterio que A-22/A-26/A-27. El cap de 10 en `pendingCompanies` es inocuo: `PendingValidations` solo renderiza 3 y linkea a `/moderacion/validaciones`. ⚠️ Los `status-summary` por dominio y los listados completos siguen haciendo falta para las pantallas de listado de cada dominio — este endpoint es solo para el dashboard. Mismo matiz que A-26/A-27: verificado contra la copia de `ENDPOINTS.md` del backend, no contra su código fuente. Sin documentar en la copia local (`docs/ENDPOINTS.md`). |
| **A-29** | ✅ | **`GET /user` PAGINA del lado del servidor, con `size` default 20** — verificado 2026-07-30 contra el **código fuente** del backend (`user/UserController.getAll`, rama `dev`), cuya firma real es `getAll(AccountStatus status, Role role, int page = 0, int size = 20) → List<UserResponse>`. ⚠️ **Las dos copias de `ENDPOINTS.md` lo documentan mal**: la del backend lo describe como lista plana con solo `status`/`role`, sin mencionar paginación — otro caso de manual para la regla de [Fuentes y precedencia](sources-and-precedence.md) (Fuente #4). Consecuencia práctica: pedirlo sin `page`/`size` devuelve **solo los primeros 20 usuarios**, en silencio y sin metadata en la respuesta que lo delate — por eso las pantallas de moderación mostraban menos alumnos/empresas de los que existen. ⚠️ **No acepta `sort`**: el orden entre páginas no está garantizado, así que un alta concurrente puede hacer que una fila aparezca en dos páginas. Por eso `features/moderacion/fetch-all-users.ts` (el recorrido compartido por las 5 pantallas de moderación que necesitan el listado entero) **deduplica por `userId`** al agotar páginas: sin eso el usuario repetido infla el `total` que alimenta la paginación de la tabla y repite la key de React. Pedirle a backend un orden estable (y, mejor aún, metadata de paginación) sigue pendiente. |
| **A-30** | ✅ | **`GET /vacancy-application/detailed`** — listado GLOBAL de postulaciones ya resuelto para la tabla de Postulaciones del admin, **confirmado 2026-07-30 contra el `docs/ENDPOINTS.md` del repo de backend** (`ucupderetos/ucu-talent-backend`, rama `dev`). Permiso 🔒 rol `ADMIN`. Response `List<ApplicationListItemResponse>`: `{ application: VacancyApplicationResponse, studentName, studentSurname, studentEmail, vacancyId, vacancyName, companyId, companyName }`. ⚠️ **No confundir con `/vacancy-application/me/detailed` (A-26), que es rol `ALUMNO`** y tiene otro shape: aquel NO trae `studentProfileId` ni `accepted` (es el listado propio del alumno); este SÍ trae los dos, porque el admin necesita identificar al postulante. Una empresa no puede llamar a ninguno de los dos. Reemplaza en `use-applications.ts` el fetch-all × 4 (`/student-profile` + `/user` + `/vacancy` + `/company`) más un `GET /vacancy-application?status=` por cada valor del enum, o sea 7 requests, por 1 sola. En el front el tipo se llama `AdminApplicationDetailedResponse` (`features/moderacion/types.ts`) — nombre propio, el del wire es `ApplicationListItemResponse`. Mismo matiz que A-26/A-27/A-28: verificado contra la copia de `ENDPOINTS.md` del backend, no contra su código fuente. Sin documentar en la copia local (`docs/ENDPOINTS.md`). |
| **A-31** | 🔄 | **No hay feed de actividad reciente cruzado entre dominios** — pedido a backend 2026-07-30, sin resolver. `GET /admin/dashboard` (A-28) devuelve totales y listados pero **nada de actividad**, y `/audit` es auditoría interna (no representa altas de negocio). Decisión del equipo: implementarlo igual, **derivándolo en el cliente**, en su propia query (`features/moderacion/hooks/use-recent-activity.ts`) y no dentro de `use-dashboard.ts` — así el payload del dashboard sigue siendo 1 request y el costo del feed queda aislado. ⚠️ **Ese costo es real**: 4 listados completos (`/student-profile` + `/company` + `/vacancy` + `/user` paginado, ver A-29) para mostrar 3 filas. Es el fetch-all que A-22/A-26/A-27/A-28/A-30 vinieron eliminando, así que se acepta como provisorio, no como patrón a copiar. **Lo que se le pide a backend**: agregar `recentActivity` a `AdminDashboardResponse` (las N altas más recientes entre alumnos/empresas/ofertas, con `occurredAt` + tipo + id, ya ordenadas del lado del servidor). El día que exista, este hook se borra entero y el componente no cambia. ⚠️ Nota aparte: la fecha de alta de alumnos y empresas sale de `User.registeredAt`, NO del perfil — perfil y cuenta son pasos separados y el perfil puede completarse mucho después. |
| **A-32** | ✅ | **Editar una vacante publicada en el pasado la RE-FECHA a hoy — comportamiento asumido, no bug.** Verificado 2026-07-30 contra el código fuente (`vacancy/VacancyServiceImpl.updateVacancy` + `dateValidation`, rama `dev`): `dateValidation` corre **siempre** al final del update, sobre la entidad ya mergeada, y contiene `if (inicio.isBefore(today)) throw` — así que una vacante cuya `publicationDate` ya pasó **no se puede editar en absoluto** mientras conserve esa fecha, y **omitir el campo del payload no ayuda**: la entidad se queda con el valor guardado y la validación falla igual (403 "La fecha de publicación no puede ser anterior a la fecha actual"). ⚠️ **Decisión del equipo (backend congelado): mandar hoy desde el front** (`publicationDateToSend` en `features/puestos/components/edit-job-form.tsx`). **Efecto colateral aceptado**: la vacante queda republicada con fecha de hoy y pierde la original — los alumnos ven otra fecha y la oferta salta al tope de "Ofertas recientes" del dashboard, que ordena por `publicationDate` DESC (A-28). Por eso el formulario **lo avisa antes de guardar** con un `FieldDescription` bajo el campo read-only; no se cambia en silencio. El piso de `closingDate` sigue a la fecha que realmente se manda (no a la guardada) y su mensaje de error cambia acorde, porque `dateValidation` también tiene `inicio.isAfter(fin)`. ⚠️ Acota el impacto: el update ya se rechaza si la vacante tiene **cualquier** postulación o si está `FINALIZADO`, así que esto solo alcanza a vacantes sin postulantes. |

## 🔴 Todavía abierto

| # | Qué falta |
|---|---|
| **A-11** | **Lo que queda del ítem de archivos.** El CV del alumno ya está implementado de las dos puntas (subida/borrado desde `/perfil` y descarga desde el detalle del postulante) — el detalle de qué archivo hace qué está en A-24, en la tabla de arriba. Sigue faltando: **(a) dónde sube la foto un ADMIN** — no tiene pantalla de perfil, el route group `(perfil)` está guardado a ALUMNO+EMPRESA; **(b) la prueba end-to-end contra el backend** — desde `localhost:3000` no hay sesión posible (cookie third-party, ver A-13) y `https://dev.ucutalent.tech` todavía no tiene este código, así que hay que verificarlo después de mergear y deployar. |
| **A-20** | **Semilla de `Area`/`Degree` en `api-dev`** — se confirma con `GET /area`/`GET /degree` logueado. |

No queda ningún ítem 🟡 (definiciones de UI sin bloqueo de backend): los dos que había
(A-06, A-07) se resolvieron contra el código fuente del backend y están en la tabla de
arriba. Por eso este documento tiene solo dos tablas y no tres.
